{
  "name": "enterprise-performance-goal-management-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Enterprise B2B Performance & Goal Management System with 360-Degree Peer Reviews",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n \"backend,frontend\" -c \"blue,green\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "db:migrate": "npm run prisma:migrate --prefix backend",
    "db:seed": "npm run seed --prefix backend",
    "db:studio": "npm run prisma:studio --prefix backend",
    "test:api": "npm run test:api --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "keywords": [
    "enterprise",
    "performance-management",
    "okr",
    "goals",
    "prisma",
    "express",
    "nextjs",
    "typescript",
    "tailwind"
  ],
  "author": "Akilidex Engineering Candidate",
  "license": "MIT"
}
